import 'package:flutter_test/flutter_test.dart';

void main() {
  // Smoke tests will be added as features stabilize.
  // The app is fully local-first (Hive) — no external services needed.
  test('placeholder', () => expect(true, isTrue));
}
